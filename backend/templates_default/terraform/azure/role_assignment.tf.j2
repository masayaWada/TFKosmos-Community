resource "azurerm_role_assignment" "{{ resource_name }}" {
  scope                = "{{ role_assignment.scope }}"
  role_definition_name = "{{ role_assignment.role_definition_name }}"
  principal_id         = "{{ role_assignment.principal_id }}"
}

